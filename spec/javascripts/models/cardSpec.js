describe("Card", function() {
    var card;

    beforeEach(function() {
        card = new Card();
    });

    it("should be faced down by default", function() {
        expect(card.get('faceDown').toBe(true);
    });

  
});
